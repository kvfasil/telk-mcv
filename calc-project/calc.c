#include <stdio.h>
int sum (int x, int y);
int sub(int ,int);

void main()
{
	printf"calculator");
	int val = sum(10, 20);
	print("%d", sub(10,5));
	printf("%d \n",val)

}

int sum (int x, int y)
{
	return (x+y);
}
int sub (int x, int y)
{
	return (x-y);
}
